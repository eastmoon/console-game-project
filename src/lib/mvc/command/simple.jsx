import BasicObject from "lib/mvc/basic/object";

export default class SimpleCommand extends BasicObject {
    // Constructor
    constructor($name = "") {
        // Call parent constructor
        super($name);
    }
    // Static method

    // Method

    // execute
    // Execute algorithm in this object.
    execute($args = null) {
        return $args;
    }
}
